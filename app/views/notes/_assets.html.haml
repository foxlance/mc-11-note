%h3 Assets for note

%ul
  - @note.assets.each do |asset|
    %li
      = link_to asset.file_url do
        = image_tag asset.file_url(:thumb), width: 120 if asset.file_url =~ /(gif|jpg|jpeg|png)$/
        = asset.file_identifier

= form_for [@note, @note.assets.build], multipart: true do |f|
  %p= f.file_field :file
  = f.button :submit, class: 'button'
