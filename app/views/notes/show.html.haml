%h1.title= @note.title
%p= simple_format(@note.content)

%p= link_to "All Notes", notes_path

%p= link_to "Edit", edit_note_path(@note)
%p= link_to "Delete", note_path(@note), method: :delete, data: {confirm: 'Are you sure you want to delete?'}
